from PySide6.QtCore import QObject


class Text(QObject):
    def __init__(self):
        super().__init__()
        self.AddNewProject = self.tr("添加新项目")
        self.EnterProjectName = self.tr("输入项目的名字")
        self.ETNOEITS = self.tr("输入这个系列一共几集")
        self.EOTOTS = self.tr("输入这个系列的原标题")
        self.ProjectName = self.tr("项目名称:")
        self.TotalEpisodes = self.tr("总集数:")
        self.OriginalTitle = self.tr("原标题:")
        self.ProjectName2 = self.tr("项目名称")
        self.PEPN = self.tr("请输入项目名称")
        self.TotalEpisodes2 = self.tr("总集数")
        self.PETNOE = self.tr("请输入总集数")
        self.OriginalTitle2 = self.tr("原标题")
        self.PEOT = self.tr("请输入原标题")
        self.EFAE = self.tr("错误：'{}' 文件夹已存在！")
        self.TEMBGT0 = self.tr("总集数必须大于0")
        self.TEMBLT1 = self.tr("总集数必须小于128")
        self.TEMBAVN = self.tr("总集数必须是有效的数字")
        self.ANPFP = self.tr("根据播放列表添加新项目")
        self.EnterPlaylistURL = self.tr("输入播放列表url")
        self.VideoListURL = self.tr("视频列表url:")
        self.VideoListURL2 = self.tr("视频列表url")
        self.PEPU = self.tr("请输入播放列表url")
        self.FileSelection = self.tr("文件选择")
        self.Settings = self.tr("设置")
        self.File = self.tr("{}文件")
        self.SelectFileTo = self.tr("选择要{}的文件")
        self.SelectFile = self.tr("选择文件...")
        self.OutputFile = self.tr("输出文件")
        self.SetSavePathForFile = self.tr("设置{}后的文件保存路径")
        self.OutputFilePath = self.tr("输出文件路径...")
        self.AddTask = self.tr("添加任务")
        self.Previous = self.tr("上一个")
        self.Next = self.tr("下一个")
        self.OSFIS = self.tr("只支持单个文件")
        self.NFANS = self.tr("不支持网络文件")
        self.FANS = self.tr("不支持文件夹")
        self.FilesAreNotSupported = self.tr("不支持{}文件")
        self.SelectFile2 = self.tr("选择{}文件")
        self.FilesAllFiles = self.tr("文件 ({});;所有文件 (*.*)")
        self.PSTOFF = self.tr("请先选择原{}文件")
        self.SaveFile = self.tr("保存文件")
        self.DuplicateTask = self.tr("重复的任务")
        self.TAS = self.tr("任务-{}-添加成功！")
        self.Success = self.tr("成功")
        self.Error = self.tr("错误")
        self.Warning = self.tr("警告")
        self.OK = self.tr("确定")
        self.Cancel = self.tr("取消")
        self.Default = self.tr("默认")
        self.InProgress = self.tr("{}中")
        self.OpenFolder = self.tr("打开文件夹")
        self.Retry = self.tr("重新")
        self.RemoveTask = self.tr("移除任务")
        self.ConfirmCancellation = self.tr("确认取消")
        self.AYSYWTCT = self.tr("确定要取消这个")
        self.Task = self.tr("任务吗？")
        self.Cancelled = self.tr("已取消")
        self.Task2 = self.tr("任务")
        self.HasBeenCancelled = self.tr("已被取消")
        self.AdvancedSettings = self.tr("高级设置")
        self.All = self.tr("全部")
        self.Completed = self.tr("-{}- {}完成")
        self.Failed = self.tr("-{}- 失败: {}")
        self.Failed2 = self.tr("{}失败")
        self.BatchDeleteFiles = self.tr("批量删除文件")
        self.SelectAll = self.tr("全选")
        self.DeselectAll = self.tr("取消全选")
        self.DeleteFiles = self.tr("删除文件")
        self.Episode = self.tr("第 {} 集 - {}")
        self.BatchAddTasks = self.tr("批量添加任务")
        self.BrowseFile = self.tr("浏览文件")
        self.FirstInput = self.tr("第一个输入")
        self.SecondInput = self.tr("第二个输入")
        self.OpenProjectPath = self.tr("打开项目路径")
        self.PinToTop = self.tr("置顶")
        self.UnlinkProject = self.tr("取消项目连接")
        self.PDP = self.tr("永久删除项目")
        self.PathDoesNotExist = self.tr("路径不存在: {}")
        self.InputContent = self.tr("输入内容")
        self.PleaseEnterContent = self.tr("请输入内容")
        self.APIKeyCannotBeEmpty = self.tr("API密钥不能为空")
        self.ABUCBE = self.tr("API基础URL不能为空")
        self.MNCBE = self.tr("模型名称不能为空")
        self.ABUMSWHOH = self.tr("API基础URL必须以http://或https://开头")
        self.OLNIPRPIO = self.tr("未安装openai库，请运行: pip install openai")
        self.PCCACS = self.tr("参数配置正确，API连接成功！")
        self.APIResponseAbnormal = self.tr("API响应异常")
        self.AKIIOHE = self.tr("API密钥无效或已过期")
        self.MDNEOABUII = self.tr("模型不存在或API基础URL错误")
        self.CCTASPCNAU = self.tr("无法连接到API服务器，请检查网络和URL")
        self.RTPCNC = self.tr("请求超时，请检查网络连接")
        self.DetectionFailed = self.tr("检测失败")
        self.Detect = self.tr("检测")
        self.TencentHunyuan = self.tr("腾讯混元")
        self.Deepseek = self.tr("Deepseek")
        self.Gemini3Flash = self.tr("Gemini 3.5 Flash")
        self.InternLM = self.tr("书生")
        self.GLM45FLASH = self.tr("GLM-4.5-FLASH")
        self.SparkLite = self.tr("Spark-Lite")
        self.BaiduERNIESpeed128K = self.tr("百度ERNIE-Speed-128K")
        self.CustomModel = self.tr("自定义模型")
        self.ChineseEnglish = self.tr("中文与英文")
        self.TraditionalChinese = self.tr("繁体中文")
        self.English = self.tr("英语")
        self.Japanese = self.tr("日语")
        self.Korean = self.tr("韩语")
        self.French = self.tr("法语")
        self.German = self.tr("德语")
        self.Spanish = self.tr("西班牙语")
        self.Portuguese = self.tr("葡萄牙语")
        self.Italian = self.tr("意大利语")
        self.Russian = self.tr("俄语")
        self.Arabic = self.tr("阿拉伯语")
        self.Dutch = self.tr("荷兰语")
        self.Greek = self.tr("希腊语")
        self.Swedish = self.tr("瑞典语")
        self.Norwegian = self.tr("挪威语")
        self.Finnish = self.tr("芬兰语")
        self.Polish = self.tr("波兰语")
        self.Czech = self.tr("捷克语")
        self.Hungarian = self.tr("匈牙利语")
        self.Romanian = self.tr("罗马尼亚语")
        self.Bulgarian = self.tr("保加利亚语")
        self.SerbianCyrillic = self.tr("塞尔维亚语(西里尔文)")
        self.SerbianLatin = self.tr("塞尔维亚语(拉丁文)")
        self.Croatian = self.tr("克罗地亚语")
        self.Slovak = self.tr("斯洛伐克语")
        self.Slovenian = self.tr("斯洛文尼亚语")
        self.Ukrainian = self.tr("乌克兰语")
        self.Belarusian = self.tr("白俄罗斯语")
        self.Albanian = self.tr("阿尔巴尼亚语")
        self.Estonian = self.tr("爱沙尼亚语")
        self.Latvian = self.tr("拉脱维亚语")
        self.Lithuanian = self.tr("立陶宛语")
        self.Icelandic = self.tr("冰岛语")
        self.Irish = self.tr("爱尔兰语")
        self.Welsh = self.tr("威尔士语")
        self.Maltese = self.tr("马耳他语")
        self.Hindi = self.tr("印地语")
        self.Urdu = self.tr("乌尔都语")
        self.Bengali = self.tr("孟加拉语")
        self.Tamil = self.tr("泰米尔语")
        self.Telugu = self.tr("泰卢固语")
        self.Marathi = self.tr("马拉地语")
        self.Thai = self.tr("泰语")
        self.Vietnamese = self.tr("越南语")
        self.Indonesian = self.tr("印度尼西亚语")
        self.Malay = self.tr("马来语")
        self.Filipino = self.tr("菲律宾语")
        self.Persian = self.tr("波斯语")
        self.Turkish = self.tr("土耳其语")
        self.Hebrew = self.tr("希伯来语")
        self.Nepali = self.tr("尼泊尔语")
        self.Sinhala = self.tr("僧伽罗语")
        self.Burmese = self.tr("缅甸语")
        self.Khmer = self.tr("高棉语")
        self.Lao = self.tr("老挝语")
        self.Mongolian = self.tr("蒙古语")
        self.Uyghur = self.tr("维吾尔语")
        self.Uzbek = self.tr("乌兹别克语")
        self.Swahili = self.tr("斯瓦希里语")
        self.Afrikaans = self.tr("南非荷兰语")
        self.Latin = self.tr("拉丁语")
        self.Sanskrit = self.tr("梵语")
        self.Maori = self.tr("毛利语")
        self.Abaza = self.tr("阿巴扎语")
        self.Adyghe = self.tr("阿迪格语")
        self.Angika = self.tr("安吉卡语")
        self.Avar = self.tr("阿瓦尔语")
        self.Azerbaijani = self.tr("阿塞拜疆语")
        self.Bhojpuri = self.tr("博杰普尔语")
        self.Bosnian = self.tr("波斯尼亚语")
        self.Chechen = self.tr("车臣语")
        self.Dargwa = self.tr("达尔格瓦语")
        self.GoanKonkani = self.tr("果阿康卡尼语")
        self.Haryanvi = self.tr("哈里亚纳语")
        self.Ingush = self.tr("印古什语")
        self.Kabardian = self.tr("卡巴尔达语")
        self.Kurdish = self.tr("库尔德语")
        self.Lak = self.tr("拉克语")
        self.Lezgi = self.tr("列兹金语")
        self.Magahi = self.tr("马加希语")
        self.Maithili = self.tr("迈蒂利语")
        self.Nagpuri = self.tr("那格浦尔语")
        self.Newar = self.tr("尼瓦尔语")
        self.Occitan = self.tr("奥克语")
        self.Pali = self.tr("巴利语")
        self.Tabassaran = self.tr("塔巴萨兰语")
        self.Balochi = self.tr("俾路支语")
        self.Bashkir = self.tr("巴什基尔语")
        self.Basque = self.tr("巴斯克语")
        self.Buryat = self.tr("布里亚特语")
        self.Catalan = self.tr("加泰罗尼亚语")
        self.Galician = self.tr("加利西亚语")
        self.Georgian = self.tr("格鲁吉亚语")
        self.Kalmyk = self.tr("卡尔梅克语")
        self.Karakalpak = self.tr("喀拉卡尔帕克语")
        self.Kazakh = self.tr("哈萨克语")
        self.Komi = self.tr("科米语")
        self.Kyrgyz = self.tr("吉尔吉斯语")
        self.Luxembourgish = self.tr("卢森堡语")
        self.Macedonian = self.tr("马其顿语")
        self.MeadowMari = self.tr("草原马里语")
        self.Moldovan = self.tr("摩尔多瓦语")
        self.Ossetic = self.tr("奥塞梯语")
        self.Quechua = self.tr("克丘亚语")
        self.Romansh = self.tr("罗曼什语")
        self.Sindhi = self.tr("信德语")
        self.Tajik = self.tr("塔吉克语")
        self.Tatar = self.tr("鞑靼语")
        self.Tuva = self.tr("图瓦语")
        self.Udmurt = self.tr("乌德穆尔特语")
        self.SakhaYakut = self.tr("萨哈语")
        self.Chinese = self.tr("中文")
        self.Indonesian2 = self.tr("印尼语")
        self.AddDownloadTask = self.tr("添加下载任务")
        self.UpdateYtDlp = self.tr("更新yt-dlp")
        self.Downloading = self.tr("下载中")
        self.PleaseEnterVideoURL = self.tr("请输入视频URL:")
        self.InputError = self.tr("输入错误")
        self.PleaseEnterAValidURL = self.tr("请输入有效的URL")
        self.PSTDTDT = self.tr("请选择要下载到的目录")
        self.ConfigurationError = self.tr("配置错误")
        self.YDPDNEPCTCPIS = self.tr("yt-dlp 路径不存在: {}\n请在设置中配置正确的路径")
        self.DownloadCompleted = self.tr("下载完成")
        self.DownloadFailed = self.tr("下载失败")
        self.TaskRemovalFailed = self.tr("任务移除失败: {}")
        self.Downloading2 = self.tr("下载中: {}%")
        self.UpdateSuccessful = self.tr("更新成功")
        self.UpdateFailed = self.tr("更新失败")
        self.VideoDownload = self.tr("视频下载")
        self.Initializing = self.tr("初始化中")
        self.CancelDownload = self.tr("取消下载")
        self.RetryDownload = self.tr("重新下载")
        self.AYSYWTCTDT = self.tr("确定要取消这个下载任务吗？")
        self.DownloadCancelled = self.tr("下载已取消")
        self.Download = self.tr("下载")
        self.EditProject = self.tr("修改项目")
        self.FolderName = self.tr("文件夹名:")
        self.Icon = self.tr("图标:")
        self.PEFN = self.tr("请输入文件夹名")
        self.Encode = self.tr("压制")
        self.VEP = self.tr("压制视频进度")
        self.OWBDH = self.tr("输出将在这里显示...")
        self.Close = self.tr("关闭")
        self.FVES = self.tr("FFmpeg 视频压制设置")
        self.FFmpegPath = self.tr("FFmpeg 路径")
        self.SelectFile3 = self.tr("选择文件")
        self.BasicVideoParameters = self.tr("基本视频参数")
        self.VideoEncoder = self.tr("视频编码器")
        self.SVEF = self.tr("选择视频编码格式")
        self.CRFQualityParameter = self.tr("CRF质量参数")
        self.Text0IL12ICRLVMBQ = self.tr("0为无损，18-28为常用范围，值越小质量越好")
        self.EncodingSpeedPreset = self.tr("编码速度预设")
        self.BBESACR = self.tr("编码速度与压缩率的平衡")
        self.VeryFast = self.tr("极快")
        self.SuperFast = self.tr("超快")
        self.VeryFast2 = self.tr("非常快")
        self.Faster = self.tr("较快")
        self.Fast = self.tr("快速")
        self.Medium = self.tr("中等")
        self.Slow = self.tr("慢速")
        self.Slower = self.tr("较慢")
        self.VerySlow = self.tr("非常慢")
        self.AudioProcessing = self.tr("音频处理")
        self.AudioProcessingMode = self.tr("音频处理模式")
        self.SAPM = self.tr("选择音频处理方式")
        self.AutoDetect = self.tr("自动检测")
        self.EncodeAudio = self.tr("编码音频")
        self.NoAudio = self.tr("无音频")
        self.CopyOriginal = self.tr("复制原始")
        self.AudioEncoder = self.tr("音频编码器")
        self.SAEF = self.tr("选择音频编码格式")
        self.AudioBitrate = self.tr("音频码率")
        self.SAEQ = self.tr("设置音频编码质量")
        self.XAP = self.tr("x264高级参数")
        self.EAP = self.tr("启用高级参数")
        self.CXEP = self.tr("自定义x264编码参数")
        self.ReferenceFrameCount = self.tr("参考帧数量")
        self.RFC11 = self.tr("参考帧数量 (1-16)")
        self.BFrameCount = self.tr("B帧数量")
        self.BFrameCount016 = self.tr("B帧数量 (0-16)")
        self.KeyframeInterval = self.tr("关键帧间隔")
        self.MKI11 = self.tr("最大关键帧间隔 (1-1000)")
        self.MKI = self.tr("最小关键帧间隔")
        self.MKI112 = self.tr("最小关键帧间隔 (1-100)")
        self.SceneChangeThreshold = self.tr("场景切换阈值")
        self.SCDT01 = self.tr("场景切换检测阈值 (0-100)")
        self.QCF = self.tr("量化器压缩因子")
        self.QCCF0010 = self.tr("量化器曲线压缩因子 (0.0-1.0)")
        self.AQM = self.tr("自适应量化模式")
        self.SAQM = self.tr("选择自适应量化模式")
        self.AQS = self.tr("自适应量化强度")
        self.AQS0020 = self.tr("自适应量化强度 (0.0-2.0)")
        self.OutputSettings = self.tr("输出设置")
        self.OutputFileFormat = self.tr("输出文件格式")
        self.SOVF = self.tr("选择输出视频格式")
        self.OverwriteOutputFile = self.tr("覆盖输出文件")
        self.OIOFAE = self.tr("如果输出文件已存在则覆盖")
        self.VideoProcessing = self.tr("视频处理")
        self.CEC = self.tr("并发压制数量")
        self.MPCWEMVS = self.tr("同时压制多个视频时的最大并行数")
        self.VideoScaling = self.tr("视频缩放")
        self.AVR = self.tr("调整视频分辨率")
        self.KeepOriginalSize = self.tr("保持原尺寸")
        self.Text720p = self.tr("720p")
        self.Text1080p = self.tr("1080p")
        self.Text1440p = self.tr("1440p")
        self.Text2160p = self.tr("2160p")
        self.Custom = self.tr("自定义")
        self.CustomSize = self.tr("自定义尺寸")
        self.SCREG11 = self.tr("设置自定义分辨率 (例如: 1920:1080)")
        self.FrameRateSettings = self.tr("帧率设置")
        self.SOVFR = self.tr("设置输出视频帧率")
        self.KOFR = self.tr("保持原帧率")
        self.Text24Fps = self.tr("24 fps")
        self.Text25Fps = self.tr("25 fps")
        self.Text30Fps = self.tr("30 fps")
        self.Text50Fps = self.tr("50 fps")
        self.Text60Fps = self.tr("60 fps")
        self.VideoBitrateLimit = self.tr("视频码率限制")
        self.SVBLEG2EMNL = self.tr("设置视频码率限制 (例如: 2M, 空表示不使用限制)")
        self.PerformanceOptions = self.tr("性能选项")
        self.EHA = self.tr("启用硬件加速")
        self.UGHAFE = self.tr("使用GPU硬件加速编码")
        self.HAT = self.tr("硬件加速类型")
        self.SHAM = self.tr("选择硬件加速方式")
        self.NVIDIACUDA = self.tr("NVIDIA CUDA")
        self.IntelQSV = self.tr("Intel QSV")
        self.DXVA2 = self.tr("DXVA2")
        self.VideoToolbox = self.tr("VideoToolbox")
        self.SelectFfmpegFile = self.tr("选择ffmpeg文件")
        self.VideoEncoding = self.tr("视频压制")
        self.FairyKekkaiWorkshop = self.tr("Fairy Kekkai Workshop")
        self.Update = self.tr("更新")
        self.Version = self.tr("版本")
        self.UpdateTime = self.tr("更新时间")
        self.FairyKekkaiWorkshop2 = self.tr("仙 · 结界工坊")
        self.Log = self.tr("Log")
        self.ClearAllLogs = self.tr("清空所有日志")
        self.RASAR = self.tr("重置所有设置并重启")
        self.AYSYWTCALFTACBU = self.tr("确定要清空所有日志文件吗？此操作不可恢复。")
        self.ClearSuccessful = self.tr("清空成功")
        self.ClearedLogFiles = self.tr("已清空 {} 个日志文件")
        self.ResetAllSettings = self.tr("重置所有设置")
        self.AYSYWTRASTAWRATACBU = self.tr(
            "确定要重置软件所有设置吗？软件将会重启，此操作不可恢复。"
        )
        self.ResetFailed = self.tr("重置失败")
        self.DownloadMissingFiles = self.tr("下载缺失的文件")
        self.OCRExtractSubtitles = self.tr("OCR提取字幕")
        self.SRES = self.tr("语音识别提取字幕")
        self.SetAsCurrentOriginal = self.tr("设为当前原文")
        self.TranslateSubtitles = self.tr("翻译字幕")
        self.OpenFileLocation = self.tr("打开文件所在路径")
        self.ConfirmDelete = self.tr("确认删除")
        self.AYSYWTDFTACBU = self.tr("确定要删除文件 '{}' 吗？此操作不可撤销。")
        self.FileDeleted = self.tr("已删除文件: {}")
        self.ErrorDeletingFile = self.tr("删除文件时出错: {}")
        self.DCFE = self.tr("下载第 {} 集封面")
        self.DRVFE = self.tr("下载第 {} 集生肉视频")
        self.SetAsCurrentOrigina2 = self.tr("已将 {} 设为当前原文")
        self.SettingFailed = self.tr("设置失败: {}")
        self.FailedToOpenFile = self.tr("打开文件失败: {}")
        self.CannotOpenFile = self.tr("无法打开文件: {}")
        self.FileUploaded = self.tr("已上传文件: {}")
        self.FileUploadFailed = self.tr("上传文件失败: {}")
        self.FeatureOverview = self.tr("功能一览")
        self.ProjectManagement = self.tr("项目管理")
        self.VYTP = self.tr("查看您的烤肉项目")
        self.DSYL = self.tr("下载您相中的系列")
        self.SubtitleExtraction = self.tr("字幕提取")
        self.ESUPE = self.tr("使用PaddleOCR引擎提取字幕")
        self.SpeechRecognition = self.tr("语音识别")
        self.ESFV = self.tr("提取视频内的人声")
        self.TESF = self.tr("翻译提取出的字幕文件")
        self.ETV = self.tr("压制烤制好的视频")
        self.Settings2 = self.tr("软件设置")
        self.CAP = self.tr("设置软件的各项参数")
        self.RequiredResources = self.tr("必要资源")
        self.FFmpegBuiltIn = self.tr("FFmpeg (已内置)")
        self.FDLSPISAD = self.tr("FFmpeg下载地址，下载后可在设置里设定路径")
        self.YtDlpBuiltIn = self.tr("yt-dlp (已内置)")
        self.YDDLSPISAD = self.tr("yt-dlp下载地址，下载后可在设置里设定路径")
        self.WMNBI = self.tr("Whisper模型 (未内置)")
        self.WMDLSPISBIMISSFBSR = self.tr(
            "Whisper模型下载地址，下载后可在设置里设定路径，目前软件内置的模型为small，对于油库里语音识别效果足够",
        )
        self.APIPlatforms = self.tr("API平台")
        self.THHLAS = self.tr("腾讯混元(hunyuan-lite)API服务")
        self.DeepSeekAPIService = self.tr("深度求索API服务")
        self.GeminiAPIService = self.tr("Gemini API服务")
        self.InternLMAPIService = self.tr("书生API服务")
        self.ZhipuAI = self.tr("智谱 AI")
        self.GLM45FLASHAPIService = self.tr("GLM-4.5-FLASH API服务")
        self.IFlytekSpark = self.tr("讯飞星火")
        self.SparkLiteAPIService = self.tr("Spark-Lite API服务")
        self.BaiduQianfan = self.tr("百度千帆")
        self.ES1AS = self.tr("ERNIE-Speed-128K API服务")
        self.UsefulWebsites = self.tr("常用网站")
        self.BVP = self.tr("哔哩哔哩视频平台")
        self.YouTubePlatform = self.tr("油管视频平台")
        self.DeepSeek = self.tr("深度求索")
        self.GitHubRepository = self.tr("GitHub代码仓库")
        self.RPI = self.tr("正在刷新项目信息...")
        self.SEF = self.tr("正在扫描剧集文件夹...")
        self.CP = self.tr("正在计算分页...")
        self.LoadComplete = self.tr("加载完成")
        self.Starting = self.tr("正在启动...")
        self.LWBDH = self.tr("日志将显示在这里")
        self.LOG = self.tr("LOG")
        self.AllLogs = self.tr("全部日志")
        self.ProjectLogs = self.tr("项目日志")
        self.DownloadLogs = self.tr("下载日志")
        self.SEL = self.tr("字幕提取日志")
        self.SRL = self.tr("语音识别日志")
        self.AITranslationLogs = self.tr("AI翻译日志")
        self.FFmpegEncodingLogs = self.tr("FFmpeg压制日志")
        self.InitializingServices = self.tr("正在初始化服务...")
        self.LoadingSettings = self.tr("正在读取设置...")
        self.BackgroundImageError = self.tr("背景图片错误")
        self.PCITIE = self.tr("请检查图片是否存在")
        self.LoadingInterface = self.tr("正在加载界面...")
        self.IST = self.tr("正在初始化系统托盘...")
        self.StartupComplete = self.tr("启动完成")
        self.Home = self.tr("主页")
        self.Project = self.tr("项目")
        self.Subtitles = self.tr("字幕")
        self.Voice = self.tr("语音")
        self.Translate = self.tr("翻译")
        self.Help = self.tr("帮助")
        self.SupportProject = self.tr("支持项目")
        self.WNMTMJUII = self.tr("现在团队人手紧缺，如果感兴趣的话请加入我们")
        self.VisitRepository = self.tr("访问仓库")
        self.MaybeLater = self.tr("下次一定")
        self.TFTASRCA = self.tr("有以下任务正在运行，确定要关闭吗？\n\n{}")
        self.ConfirmClose = self.tr("确认关闭")
        self.DownloadTasks = self.tr("下载任务: {} 个")
        self.TranslationTasks = self.tr("翻译任务: {} 个")
        self.OCRTasks = self.tr("OCR任务: {} 个")
        self.EncodingTasks = self.tr("压制任务: {} 个")
        self.TranscriptionTasks = self.tr("识别任务: {} 个")
        self.NewVersionDetected = self.tr("检测到新版本")
        self.NewVersion = self.tr("新版本")
        self.ADYWTDI = self.tr("可用，你是否要下载新版本？")
        self.NoNewVersion = self.tr("没有新版本")
        self.FKWIUTD = self.tr("Fairy Kekkai Workshop 已是最新版本")
        self.DownloadComplete = self.tr("下载完成 -{}-")
        self.DownloadFailed2 = self.tr("下载失败 -{}-")
        self.SRC = self.tr("视频字幕识别完成 -{}-")
        self.SRF = self.tr("视频字幕识别失败 -{}-")
        self.TranslationComplete = self.tr("翻译完成 -{}-")
        self.TranslationFailed = self.tr("翻译失败 -{}-")
        self.EncodingComplete = self.tr("压制完成 -{}-")
        self.EncodingFailed = self.tr("压制失败 -{}-")
        self.UploadComplete = self.tr("上传完成 -{}-")
        self.UploadFailed = self.tr("上传失败 -{}-")
        self.SRC2 = self.tr("语音识别完成 -{}-")
        self.SRF2 = self.tr("语音识别失败 -{}-")
        self.OCRSettings = self.tr("OCR 设置")
        self.PaddleOCRPath = self.tr("PaddleOCR 路径")
        self.SelectFolder = self.tr("选择文件夹")
        self.TimeSettings = self.tr("时间设置")
        self.StartTime = self.tr("开始时间")
        self.STSTFVPEG00O124 = self.tr(
            "设置视频处理的起始时间点 (例如: 0:00 或 1:23:45)"
        )
        self.EndTime = self.tr("结束时间")
        self.STETFVPEG01O235 = self.tr(
            "设置视频处理的结束时间点 (例如: 0:10 或 2:34:56)"
        )
        self.ThresholdSettings = self.tr("阈值设置")
        self.SimilarityThreshold = self.tr("相似度阈值")
        self.STBSF01 = self.tr("字幕帧之间的相似度阈值 (0-100)")
        self.SSIMThreshold = self.tr("SSIM阈值")
        self.SSITFDFC01 = self.tr("结构相似性指数阈值，用于判断帧间变化 (0-100)")
        self.ProcessingParameters = self.tr("处理参数")
        self.MaxMergeInterval = self.tr("最大合并间隔")
        self.MMIBSS0110 = self.tr("相邻字幕片段的最大合并间隔 (0.1-10.0 秒)")
        self.MaxOCRImageWidth = self.tr("最大OCR图像宽度")
        self.MIWFOP14P = self.tr("OCR处理时图像的最大宽度 (100-4096 像素)")
        self.SkipFrames = self.tr("跳过的帧数")
        self.FTSDPFS01 = self.tr("处理时跳过的帧数，用于提高处理速度 (0-100)")
        self.MinSubtitleDuration = self.tr("最小字幕持续时间")
        self.MSDSOF0110 = self.tr(
            "字幕的最小持续时间，短于此时间的字幕将被过滤 (0.1-10.0 秒)"
        )
        self.FeatureToggles = self.tr("功能开关")
        self.EGA = self.tr("启用GPU加速")
        self.UGFFOP = self.tr("使用GPU进行OCR处理以提高速度")
        self.EnableDualAreaOCR = self.tr("启用双区域OCR")
        self.SSTASP = self.tr("支持同时处理两个区域的字幕")
        self.UsePostProcessing = self.tr("使用后期处理")
        self.APPOTOR = self.tr("对OCR结果进行后期处理优化")
        self.UHPM = self.tr("使用高精度模型")
        self.UABMFOP = self.tr("使用更好的模型进行OCR处理")
        self.PSPE = self.tr("请选择paddleocr.exe")
        self.PEPMNCCC = self.tr("paddleocr.exe 路径不能包含中文字符")
        self.OK2 = self.tr("确认")
        self.PleaseSelectOCRModel = self.tr("请选择OCR.model")
        self.OMPMNCCC = self.tr("OCR.model 路径不能包含中文字符")
        self.PSVCE = self.tr("请选择videocr-cli.exe")
        self.VCEPMNCCC = self.tr("videocr-cli.exe 路径不能包含中文字符")
        self.PSSETF = self.tr("请选择字幕提取临时文件夹temp")
        self.TFPMNCCC = self.tr("字幕提取临时文件夹temp 路径不能包含中文字符")
        self.Extract = self.tr("提取")
        self.Extracting = self.tr("提取中")
        self.S13PVF = self.tr("步骤1/3: 正在处理视频帧…")
        self.S13PVF2 = self.tr("步骤1/3: 正在处理视频帧… {}/{}")
        self.Step23DetectingText = self.tr("步骤2/3: 正在进行文本检测 {}/{}")
        self.S23ADF = self.tr("步骤2/3: 正在分析检测帧 {}/{}")
        self.S33ROOI = self.tr("步骤3/3: 正在对图像进行OCR识别 {}/{}")
        self.GSF = self.tr("正在生成字幕文件...")
        self.FTPP = self.tr("解析进度失败: {}")
        self.GoToFirstPage = self.tr("跳转到第一页")
        self.GoToLastPage = self.tr("跳转到最后一页")
        self.PreviousPage = self.tr("上一页")
        self.NextPage = self.tr("下一页")
        self.JumpTo = self.tr("跳转到")
        self.PageOfPagesTotal = self.tr("页 共计 {} 页")
        self.PromptWritingHelp = self.tr("Prompt编写帮助")
        self.OLRTSLTLRTTLCRTTTBT = self.tr(
            "{origin_lang}表示原语言\n{target_lang}表示翻译后的语言\n{content}表示待翻译的文本",
        )
        self.OpenProject = self.tr("打开项目")
        self.ProjectInfoUpdated = self.tr("已修改项目信息")
        self.LoadingProject = self.tr("正在加载项目...")
        self.StartDownload = self.tr("开始下载")
        self.DownloadingImage = self.tr("正在下载图片...")
        self.ImageDownloadedTo = self.tr("图片已下载到: {}")
        self.ErrorRefreshing = self.tr("刷新时出错: {}")
        self.BackToProjectList = self.tr("返回项目列表")
        self.RefreshProjectList = self.tr("刷新项目列表")
        self.BatchTasks = self.tr("批量任务")
        self.BADTTET = self.tr("批量添加下载/语音识别/翻译/压制任务")
        self.BatchDelete = self.tr("批量删除")
        self.DEFBTIB = self.tr("按文件类型批量删除剧集文件")
        self.EpisodesTotalPage = self.tr("共 {} 集，第 {}/{} 页")
        self.InsertNewEpisode = self.tr("插入新的一集")
        self.FileListRefreshed = self.tr("已刷新文件列表")
        self.Info = self.tr("提示")
        self.NEICP = self.tr("当前项目没有剧集")
        self.FailedToAddEpisode = self.tr("第 {} 集添加失败: {}")
        self.TasksAdded = self.tr("已添加 {} 个任务")
        self.NoTasksSelected = self.tr("没有选中任何任务")
        self.NoFilesSelected = self.tr("没有选中任何文件")
        self.ConfirmBatchDelete = self.tr("确认批量删除")
        self.AYSYWTDTCBU = self.tr("确定要批量删除 {} 个 '{}' 吗？此操作不可撤销。")
        self.Deleted = self.tr("已删除 {} 个 '{}'")
        self.BatchDeleteFailed = self.tr("批量删除失败: {}")
        self.INEBT = self.tr("在这之前插入新的一集")
        self.DTEI = self.tr("删除这一集(不可撤销)")
        self.EETAVU = self.tr("编辑本集标题和视频url")
        self.OpenEpisodeLink = self.tr("打开本集链接: {}")
        self.InsertAtEpisode = self.tr("在第 {} 集处插入")
        self.TranslatedTitle = self.tr("翻译后标题:")
        self.VideoURL = self.tr("视频URL:")
        self.PETT = self.tr("请输入翻译后标题")
        self.PleaseEnterVideoURL2 = self.tr("请输入视频URL")
        self.NewEpisodeInserted = self.tr("已插入新的一集")
        self.UnknownError = self.tr("未知错误")
        self.AddFailed = self.tr("添加失败: {}")
        self.AYSYWTDTETCBU = self.tr("确定要删除这一集吗？此操作不可撤销。")
        self.Processing = self.tr("处理中")
        self.DeletingEpisode = self.tr("正在删除第 {} 集...")
        self.EpisodeDeleted = self.tr("已删除第 {} 集")
        self.DeleteFailed = self.tr("删除失败: {}")
        self.ETAVUFE = self.tr("编辑第 {} 集的标题和视频URL")
        self.Title = self.tr("标题:")
        self.URL = self.tr("URL:")
        self.PleaseEnterTitle = self.tr("请输入标题")
        self.PleaseEnterVideoURL3 = self.tr("请输入视频url")
        self.ETAUU = self.tr("编辑第 {} 集标题和视频url成功")
        self.CopyingFile = self.tr("正在复制文件 {}...")
        self.FileUploadedTo = self.tr("已上传文件到: {}/{}")
        self.NewProjectCreated = self.tr("已创建新项目: {}")
        self.IsNotAValidProject = self.tr("{} 并不是一个合法的项目")
        self.SelectImportMode = self.tr("选择导入模式")
        self.CTEPF = self.tr("是否要把整个项目文件夹复制过来")
        self.Copy = self.tr("复制")
        self.LinkOnly = self.tr("只连接路径")
        self.Added = self.tr("已添加 {}")
        self.PathImported = self.tr("已导入此路径 {}")
        self.ProjectCreated = self.tr("项目创建完毕")
        self.Failed3 = self.tr("失败")
        self.ProjectListRefreshed = self.tr("已刷新项目列表")
        self.ProjectDeleted = self.tr("项目 {} 已删除")
        self.DeleteProjectFailed = self.tr("删除项目失败: {}")
        self.UnlinkedProject = self.tr("已解除项目 {} 的连接")
        self.ProjectPinnedToTop = self.tr("项目已置顶")
        self.GotIt = self.tr("我知道了")
        self.Cover = self.tr("封面")
        self.OriginalVideo = self.tr("原视频")
        self.TranslatedVideo = self.tr("翻译后的视频")
        self.OriginalSubtitle = self.tr("原字幕")
        self.TranslatedSubtitle = self.tr("翻译后的字幕")
        self.OpenedSuccessfully = self.tr("打开成功")
        self.ProjectOpened = self.tr("已打开项目 {}")
        self.Title2 = self.tr("* 标题")
        self.PleaseEnterTheTitle = self.tr("请输入稿件标题")
        self.Type = self.tr("  类型")
        self.Original = self.tr("自制")
        self.Repost = self.tr("转载")
        self.YouCanAddMoreTags = self.tr("还可以添加{}个标签")
        self.TagLimitReached = self.tr("已达到标签上限")
        self.PETCT = self.tr("按回车键Enter创建标签")
        self.Upload = self.tr("上传")
        self.VideoFile = self.tr("视频文件")
        self.CoverFile = self.tr("封面文件")
        self.PSACI = self.tr("请选择你的封面")
        self.SelectVideoFile = self.tr("选择视频文件")
        self.SelectCoverFile = self.tr("选择封面文件")
        self.BasicSettings = self.tr("基本设置")
        self.PFICSF = self.tr("请先填写Cookie：Sessdata")
        self.PFICBF = self.tr("请先填写Cookie：BiliJct")
        self.PFICBF2 = self.tr("请先填写Cookie：Buvid3")
        self.PSAVF = self.tr("请选择视频文件")
        self.PSACF = self.tr("请选择封面文件")
        self.PETRL = self.tr("请输入转载链接")
        self.PleaseEnterTags = self.tr("请输入标签")
        self.STMBITF = self.tr("选择的时间必须大于现在的时间")
        self.VideoUploadProgress = self.tr("上传视频进度")
        self.UploadSettings = self.tr("上传设置")
        self.UploadVideoPath = self.tr("upload-video 路径")
        self.CRFU = self.tr("Cookie (必填 否则无法上传)")
        self.SESSDATA = self.tr("SESSDATA")
        self.SetYourSESSDATA = self.tr("设置你的SESSDATA")
        self.BILIJCT = self.tr("BILI_JCT")
        self.SetYourBILIJCT = self.tr("设置你的BILI_JCT")
        self.BUVID3 = self.tr("BUVID3")
        self.SetYourBUVID3 = self.tr("设置你的BUVID3")
        self.SUVF = self.tr("选择upload-video文件")
        self.VideoUpload = self.tr("视频上传")
        self.Personalization = self.tr("个性化")
        self.ApplicationTheme = self.tr("应用主题")
        self.AAA = self.tr("调整应用的外观")
        self.Light = self.tr("浅色")
        self.Dark = self.tr("深色")
        self.FollowSystem = self.tr("跟随系统设置")
        self.ThemeColor = self.tr("主题色")
        self.AdjustThemeColor = self.tr("调整应用的主题颜色")
        self.SeafoamGreen = self.tr("海沫绿")
        self.WindowStyle = self.tr("窗口类型")
        self.AdjustWindowStyle = self.tr("调整应用的窗口类型")
        self.MSFluentWindow = self.tr("MSFluentWindow")
        self.FluentWindow = self.tr("FluentWindow")
        self.SplitFluentWindow = self.tr("SplitFluentWindow")
        self.InterfaceScaling = self.tr("界面缩放")
        self.ACAFS = self.tr("调整组件和字体的大小")
        self.Language = self.tr("语言")
        self.SetInterfaceLanguage = self.tr("设置界面语言")
        self.CloseDirectly = self.tr("直接关闭")
        self.EODDC = self.tr("启用或禁用直接关闭应用")
        self.BackgroundImage = self.tr("背景图片")
        self.EODBI = self.tr("启用或禁用应用背景图片")
        self.SBI = self.tr("选择背景图片")
        self.BackgroundOpacity = self.tr("背景透明度")
        self.ABO = self.tr("调整背景图片的透明度")
        self.IPPIPD = self.tr("项目详情页数量")
        self.AdjustItemsPerPage = self.tr("调整项目详情页项目数量")
        self.DetectPrograms = self.tr("检测程序")
        self.ADAUPP = self.tr("自动检测并更新程序路径")
        self.About = self.tr("关于")
        self.CheckForUpdates = self.tr("检查更新")
        self.Copyleft = self.tr("Copyleft")
        self.CurrentVersion = self.tr("当前版本")
        self.ViewTutorial = self.tr("查看新手引导")
        self.Tutorial = self.tr("新手引导")
        self.ReplayTheTutorial = self.tr("重新查看软件使用教程")
        self.STEAR = self.tr("配置在重启软件后生效")
        self.SelectYtDlpFile = self.tr("选择ytdlp文件")
        self.DetectionSuccessful = self.tr("检测成功")
        self.PathSetTo = self.tr("{}路径已设置为{}")
        self.NotFoundDownloadIt = self.tr("未检测到{}程序，是否要下载")
        self.GoToDownload = self.tr("前往下载")
        self.Detecting = self.tr("检测中...")
        self.ShowHideWindow = self.tr("显示/隐藏界面")
        self.Exit = self.tr("退出")
        self.MTST = self.tr("程序已最小化到系统托盘")
        self.WTFKW = self.tr("欢迎使用 Fairy Kekkai Workshop")
        self.AToolDesignedForTouh = self.tr(
            "这是一个专为东方Project视频翻译设计的工具，支持视频下载、OCR识别、语音识别、AI翻译、视频压制等功能。主页的「关于」卡片中还可以查看日志、一键清空日志或重置所有设置。",
        )
        self.ClickNewProjectToCre = self.tr(
            "点击「新建项目」创建新的翻译项目，或点击「导入项目」导入已有项目。每个项目对应一个视频系列。",
        )
        self.DownloadVideo = self.tr("下载视频")
        self.EnterTheVideoURLInTh = self.tr(
            "在下载界面输入视频URL，支持YouTube、Bilibili等平台。可选择清晰度和下载格式。",
        )
        self.OCRRecognition = self.tr("OCR识别")
        self.SAVFITSICSOFSRRCPMP = self.tr(
            "在字幕界面选择视频文件，点击「开始OCR」进行字幕识别。需要配置PaddleOCR模型路径。",
        )
        self.SelectAVideoOrAudioF = self.tr(
            "在识别界面选择视频或音频文件，使用 Whisper 模型将语音转写为字幕。可在设置中选择模型、语言、输出格式与 GPU 加速。",
        )
        self.AITranslation = self.tr("AI翻译")
        self.SelectAnSRTFileInThe = self.tr(
            "在翻译界面选择SRT文件，配置AI模型和API Key，点击「开始翻译」进行翻译。支持多轮对话保持上下文。",
        )
        self.SelectAVideoFileInTh = self.tr(
            "在压制界面选择视频文件，将字幕烤制进视频并压制输出。可在设置中调整编码器、CRF、分辨率等参数。",
        )
        self.SC = self.tr("设置配置")
        self.CAKMPDPEISPCBCBFU = self.tr(
            "在设置界面配置各种API Key、模型路径、下载参数等。首次使用请先完成基础配置。",
        )
        self.Previous2 = self.tr("上一步")
        self.Skip = self.tr("跳过")
        self.Next2 = self.tr("下一步")
        self.Finish = self.tr("完成")
        self.NewProject = self.tr("新建项目")
        self.ImportProject = self.tr("导入项目")
        self.CPFVL = self.tr("根据视频列表创建项目")
        self.TranslationProgress = self.tr("翻译进度")
        self.TTWBDH = self.tr("翻译文本将在这里显示...")
        self.AISettings = self.tr("AI设置")
        self.AIParameters = self.tr("AI参数")
        self.AITemperature = self.tr("AI温度")
        self.AATGT02 = self.tr("调整AI生成文本的温度(0~2)")
        self.PromptTemplate = self.tr("Prompt模板")
        self.SYPT = self.tr("设置你的Prompt模板")
        self.DeepseekAPIKey = self.tr("Deepseek Api Key")
        self.SYDAK = self.tr("设置你的Deepseek Api Key")
        self.ZhipuGLM45FLASH = self.tr("智谱 GLM-4.5-FLASH")
        self.ZG45FAK = self.tr("智谱 GLM-4.5-FLASH Api Key")
        self.SYG45FAK = self.tr("设置你的GLM-4.5-FLASH Api Key")
        self.XunfeiSparkLite = self.tr("讯飞 Spark Lite")
        self.XSLAP = self.tr("讯飞 Spark Lite Api Password")
        self.SYXSLAP = self.tr("设置你的讯飞 Spark Lite Api Password")
        self.TencentHunyuanAPIKey = self.tr("腾讯混元 Api Key")
        self.SYTHAK = self.tr("设置你的腾讯混元 Api Key")
        self.InternLMAPIKey = self.tr("书生 Api Key")
        self.SYIAK = self.tr("设置你的书生 Api Key")
        self.BES1NR = self.tr("百度ERNIE-Speed-128K (不推荐使用)")
        self.BES1AK = self.tr("百度ERNIE-Speed-128K Api Key")
        self.SYBES1AK = self.tr("设置你的百度ERNIE-Speed-128K Api Key")
        self.Gemini = self.tr("Gemini")
        self.Gemini3FlashAPIKey = self.tr("Gemini 3.5 Flash Api Key")
        self.SYG3FAK = self.tr("设置你的Gemini 3.5 Flash Api Key")
        self.CMOCA = self.tr("自定义模型 (支持OpenAI兼容API)")
        self.EnableCustomModel = self.tr("启用自定义模型")
        self.ECMS = self.tr("启用自定义模型支持")
        self.ModelName = self.tr("模型名称")
        self.SetCustomModelName = self.tr("设置自定义模型名称")
        self.APIKey = self.tr("API密钥")
        self.SetCustomModelAPIKey = self.tr("设置自定义模型的API密钥")
        self.APIBaseURL = self.tr("API基础URL")
        self.SCMABU = self.tr("设置自定义模型的API基础URL")
        self.ModelEndpoint = self.tr("模型端点")
        self.SMEODTMN = self.tr("设置模型端点(可选，默认使用模型名称)")
        self.Translating = self.tr("翻译中")
        self.TranslationComplete2 = self.tr("翻译完成")
        self.TranslationComplete3 = self.tr("-{}- 翻译完成")
        self.TranslationFailed2 = self.tr("翻译失败")
        self.SourceLanguage = self.tr("原文语言")
        self.SSTL = self.tr("选择字幕文本的语言")
        self.TargetLanguage = self.tr("翻译语言")
        self.SelectTargetLanguage = self.tr("选择翻译后的语言")
        self.AIModel = self.tr("AI模型")
        self.SelectAIModel = self.tr("选择AI模型")
        self.EnableContext = self.tr("启用上下文关联")
        self.WEATRSSWDEBTI = self.tr("开启后AI翻译将参考前后字幕，关闭则每批独立翻译")
        self.DeepseekModel = self.tr("Deepseek模型")
        self.SDMV = self.tr("选择Deepseek模型版本")
        self.DeepThinking = self.tr("深度思考")
        self.EDDTM = self.tr("启用Deepseek深度思考模式")
        self.SRTContentPreview = self.tr("SRT 内容预览")
        self.Text0SubtitlesTotal = self.tr("共 0 条字幕")
        self.No = self.tr("序号")
        self.Timeline = self.tr("时间轴")
        self.Content = self.tr("内容")
        self.PFIYDAKF = self.tr("请先填写您的DeepSeek API Key")
        self.PFIYG45FAKF = self.tr("请先填写您的GLM-4.5-FLASH API Key")
        self.PFIYSLAKF = self.tr("请先填写您的Spark-Lite API Key")
        self.PFIYTHAKF = self.tr("请先填写您的腾讯混元 API Key")
        self.PFIYIAKF = self.tr("请先填写您的书生 API Key")
        self.PFIYBES1AKF = self.tr("请先填写您的百度ERNIE-Speed-128K API Key")
        self.PFIYG3FAKF = self.tr("请先填写您的Gemini 3.5 Flash API Key")
        self.PECMISF = self.tr("请先在设置中启用自定义模型")
        self.PFIYCMAKF = self.tr("请先填写您的自定义模型API密钥")
        self.PFIYCMABUF = self.tr("请先填写您的自定义模型API基础URL")
        self.PFIYCMNF = self.tr("请先填写您的自定义模型名称")
        self.SATLATS = self.tr("原语言和目标语言相同")
        self.SubtitlesTotal = self.tr("共 {} 条字幕")
        self.VPACBBTSVF = self.tr("视频预览区域\n\n点击浏览按钮选择视频文件")
        self.SelectArea = self.tr("框选区域")
        self.ClearSelection = self.tr("清除框选")
        self.SCNS = self.tr("框选区域坐标: 未选择")
        self.SCS = self.tr("框选区域坐标: 正在选择...")
        self.FTLVF = self.tr("无法加载视频帧")
        self.AreaCoordinates = self.tr("区域 {} 坐标")
        self.Width = self.tr("宽度:")
        self.Height = self.tr("高度:")
        self.Apply = self.tr("应用")
        self.AreaXYWH = self.tr("区域{}: x={}, y={}, w={}, h={}")
        self.ExtractSubtitles = self.tr("提取字幕")
        self.RecognitionLanguage = self.tr("识别语言")
        self.VideoPreview = self.tr("视频预览")
        self.Frame = self.tr("帧: -/-")
        self.Time = self.tr("时间: -/-")
        self.PLWBDH = self.tr("处理日志将显示在这里...")
        self.ParameterAdjustmentT = self.tr(
            "参数调整建议：\n• 漏句 → 降低「跳过的帧数」，提高「SSIM阈值」，降低「最大图像宽度」至 720\n• 误识别多 → 提高「置信度阈值」至 0.5~0.7，过滤低质量识别结果\n• 字幕断句多 → 提高「相似度阈值」至 75~85，增大「最大合并间隔」至 0.2~0.5\n以上参数均在「高级设置」中调整",
        )
        self.ClearLogs = self.tr("清空日志")
        self.VLS = self.tr("成功加载视频: {}")
        self.TotalFramesFPS2f = self.tr("总帧数: {}, FPS: {:.2f}")
        self.FailedToLoadVideo = self.tr("加载视频失败: {}")
        self.Frame2 = self.tr("帧: {}/{}")
        self.Time2 = self.tr("时间: {}/{}")
        self.PaddleocrExeNotFound = self.tr("paddleocr.exe不存在: {}")
        self.PPMNCCC = self.tr("PaddleOCR路径 {} 不能包含中文字符")
        self.SFPMNCCC = self.tr("支持文件夹路径 {} 不能包含中文字符")
        self.TFPMNCCC2 = self.tr("临时文件夹路径 {} 不能包含中文字符")
        self.PPDNE = self.tr("PaddleOCR路径不存在: {}")
        self.SFPDNE = self.tr("支持文件夹路径不存在: {}")
        self.PSAVFF = self.tr("请先选择视频文件")
        self.PSTOFP = self.tr("请设置输出文件路径")
        self.PSSAF = self.tr("请先框选字幕区域")
        self.UsingCustomAreaX = self.tr("使用自定义区域: {}, {}, {}x{}")
        self.Recognize = self.tr("识别")
        self.SLTR = self.tr("选择要识别的语言")
        self.OutputFormat = self.tr("输出格式")
        self.SSOF = self.tr("选择字幕输出格式")
        self.BeforeUsingThisFeatu = self.tr(
            "使用此功能前需<a href=\"https://pan.xunlei.com/s/VOu1R3aOfz05uqcbNUBSnEFSA1?pwd=62cr#\" style=\"color: #0078d4;\">手动下载模型</a>后设置模型路径。<b>注意</b>: 若是超过10分钟的视频强烈建议更换medium模型，超过30分钟的视频更换large模型。更多模型可前往<a href='https://huggingface.co/ggerganov/whisper.cpp/tree/main' style='color: #0078d4;'>https://huggingface.co/ggerganov/whisper.cpp/tree/main</a>下载，语音设置不推荐选择自动检测。目前模型存在幻觉，可能会生成一些重复文本，剪辑时请注意。",
        )
        self.WCPDNE = self.tr("Whisper CLI路径 {} 不存在")
        self.WMPDNE = self.tr("Whisper模型路径 {} 不存在")
        self.SRP = self.tr("语音识别进度")
        self.WSRS = self.tr("Whisper 语音识别设置")
        self.ModelPath = self.tr("模型路径")
        self.SelectModelFile = self.tr("选择模型文件")
        self.ProgramPath = self.tr("程序路径")
        self.SelectProgram = self.tr("选择程序")
        self.LanguageSettings = self.tr("语言设置")
        self.GPUAcceleration = self.tr("GPU 加速")
        self.EGA2 = self.tr("启用 GPU 加速")
        self.UGFSRA = self.tr("使用 GPU 加速语音识别")
        self.SWMF = self.tr("选择 Whisper 模型文件")
        self.SWMEP = self.tr("选择 Whisper main.exe 程序")
        self.Recognizing = self.tr("识别中")
        self.YDDS = self.tr("YT-DLP 下载设置")
        self.YTDLPPath = self.tr("YT-DLP 路径")
        self.DFQ = self.tr("下载格式与质量")
        self.VideoQuality = self.tr("视频质量")
        self.SVR = self.tr("选择视频分辨率")
        self.Text4K2160p = self.tr("4K (2160p)")
        self.Text2K1440p = self.tr("2K (1440p)")
        self.FullHD1080p = self.tr("全高清 (1080p)")
        self.HD720p = self.tr("高清 (720p)")
        self.SD480p = self.tr("标清 (480p)")
        self.Smooth360p = self.tr("流畅 (360p)")
        self.BestQuality = self.tr("最佳质量")
        self.WorstQuality = self.tr("最差质量")
        self.ProxySettings = self.tr("代理设置")
        self.SystemProxy = self.tr("系统代理")
        self.WTUSDP = self.tr("是否启用系统默认代理")
        self.CustomProxy = self.tr("自定义代理")
        self.SCPA = self.tr("设置自定义的代理地址")
        self.SubtitleSettings = self.tr("字幕设置")
        self.DownloadSubtitles = self.tr("下载字幕")
        self.ADVS = self.tr("自动下载视频字幕")
        self.SubtitleLanguage = self.tr("字幕语言")
        self.SSLSMLWC = self.tr("设置字幕语言，多个语言用逗号分隔")
        self.EmbedSubtitles = self.tr("内嵌字幕")
        self.ESIVF = self.tr("将字幕嵌入视频文件中")
        self.MetadataThumbnails = self.tr("元数据与缩略图")
        self.DownloadThumbnails = self.tr("下载缩略图")
        self.DVT = self.tr("下载视频缩略图")
        self.EmbedThumbnails = self.tr("内嵌缩略图")
        self.ETIVF = self.tr("将缩略图嵌入视频文件中")
        self.DownloadMetadata = self.tr("下载元数据")
        self.DVMI = self.tr("下载视频元数据信息")
        self.WriteDescription = self.tr("写入描述")
        self.WVDTSF = self.tr("将视频描述写入单独文件")
        self.WriteInfoJSON = self.tr("写入信息JSON")
        self.WVITJF = self.tr("将视频信息写入JSON文件")
        self.WriteComments = self.tr("写入注释")
        self.WVCI = self.tr("写入视频注释信息")
        self.DownloadControl = self.tr("下载控制")
        self.ConcurrentDownloads = self.tr("并发下载数量")
        self.MNOSD = self.tr("同时下载的最大视频数量")
        self.RetryCount = self.tr("重试次数")
        self.NORODF = self.tr("下载失败时的重试次数")
        self.DTS = self.tr("下载超时(秒)")
        self.SDTD = self.tr("下载超时时间设置")
        self.RateLimiting = self.tr("限速下载")
        self.EDRL = self.tr("启用下载速率限制")
        self.MaxDownloadRate = self.tr("最大下载速率")
        self.SMDREG11 = self.tr("设置最大下载速率 (如: 10M, 1K)")
        self.SkipExistingFiles = self.tr("跳过已存在文件")
        self.ARDEF = self.tr("避免重复下载已存在的文件")
        self.OutputTemplate = self.tr("输出模板")
        self.SOFT = self.tr("设置输出文件名模板")
        self.UseCookies = self.tr("使用Cookies")
        self.UCFFD = self.tr("使用cookies文件进行下载")
        self.SelectCookiesFile = self.tr("选择Cookies文件")
        self.TextFilesTxtAllFiles = self.tr("Text Files (*.txt);;All Files (*)")
        self.Waiting = self.tr("等待中")
        self.Cancelling = self.tr("正在取消...")
        self.TextAuto001 = self.tr("程序错误")
        self.TextAuto002 = self.tr("程序运行时发生未处理的错误")
        self.TextAuto003 = self.tr("启动错误")
        self.TextAuto004 = self.tr("应用程序启动失败")
        self.TextAuto005 = self.tr("已完成")
        self.TextAuto006 = self.tr("{}完成")
        self.TextAuto007 = self.tr("置信度阈值")
        self.TextAuto008 = self.tr("低于此置信度的OCR结果将被过滤 (0.0-1.0, 0=不过滤)")
        self.TextAuto009 = self.tr("已创建项目文件夹,正在下载封面")
        self.TextAuto010 = self.tr("封面下载完成,开始添加下载视频任务")
        self.TextAuto011 = self.tr("已完成项目初始化！")
        self.TextAuto012 = self.tr("网址错误")
        self.TextAuto013 = self.tr("请求超时")
        self.TextAuto014 = self.tr("图片成功下载并保存到: {}")
        self.TextAuto015 = self.tr("下载图片时出错: {}")
        self.TextAuto016 = self.tr("yt-dlp不存在: {}")
        self.TextAuto017 = self.tr("压制已取消")
        self.TextAuto018 = self.tr("ffmpeg.exe不存在: {}")
        self.TextAuto019 = self.tr("正在取消OCR处理...\n")
        self.TextAuto020 = self.tr("OCR处理已取消")
        self.TextAuto021 = self.tr("OCR处理完成\n")
        self.TextAuto022 = self.tr("已强制终止OCR进程及其子进程")
        self.TextAuto023 = self.tr("OCR处理失败:\n{}\n")
        self.TextAuto024 = self.tr("已清理临时目录: {}\n")
        self.TextAuto025 = self.tr("终止进程失败: {}")
        self.TextAuto026 = self.tr("清理临时目录失败: {}\n")
        self.TextAuto027 = self.tr("Cookie 配置错误或已过期")
        self.TextAuto028 = self.tr("进程启动失败")
        self.TextAuto029 = self.tr("进程崩溃")
        self.TextAuto030 = self.tr("进程超时")
        self.TextAuto031 = self.tr("写入错误")
        self.TextAuto032 = self.tr("读取错误")
        self.TextAuto033 = self.tr("上传已取消")
        self.TextAuto034 = self.tr("上传完成")
        self.TextAuto035 = self.tr("网络连接错误")
        self.TextAuto036 = self.tr("正在准备上传...")
        self.TextAuto037 = self.tr("准备上传")
        self.TextAuto038 = self.tr("视频文件不存在或格式错误")
        self.TextAuto039 = self.tr("进程错误: {}")
        self.TextAuto040 = self.tr("上传失败，错误码: {}")
        self.TextAuto041 = self.tr("预上传失败")
        self.TextAuto042 = self.tr("upload-video.exe不存在: {}")
        self.TextAuto043 = self.tr("视频上传失败: {}")
        self.TextAuto044 = self.tr("开始上传视频...")
        self.TextAuto045 = self.tr("开始上传")
        self.TextAuto046 = self.tr("上传中")
        self.TextAuto047 = self.tr("未知")
        self.TextAuto048 = self.tr("上传失败")
        self.TextAuto049 = self.tr("上传速度: {}")
        self.TextAuto050 = self.tr("上传中 {}%")
        self.TextAuto051 = self.tr("视频上传完成")
        self.TextAuto052 = self.tr("正在提交信息...")
        self.TextAuto053 = self.tr("提交视频信息...")
        self.TextAuto054 = self.tr("提交信息")
        self.TextAuto055 = self.tr("提交失败")
        self.TextAuto056 = self.tr("上传成功")
        self.TextAuto057 = self.tr("任务已取消")
        self.TextAuto058 = self.tr("翻译进度: {}%")
        self.TextAuto059 = self.tr("不支持的AI模型: {}")
        self.TextAuto060 = self.tr("翻译失败: {}")
        self.TextAuto061 = self.tr("后处理失败: {}")
        self.TextAuto062 = self.tr("转录已取消")
        self.TextAuto063 = self.tr("转录完成")
        self.TextAuto064 = self.tr("main.exe 不存在: {}")
        self.TextAuto065 = self.tr("-{}- 下载完成")
        self.TextAuto066 = self.tr("压制中")
        self.Danish = self.tr("丹麦语")
        self.TextAuto067 = self.tr("请输入视频URL:")
